<!DOCTYPE html>
<div style="position:fixed;top:0;left:0;width:100vw;background:#c00;color:#fff;text-align:center;font-weight:bold;z-index:9999;padding:8px 0;letter-spacing:2px;">EM DESENVOLVIMENTO</div>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Dashboard JSCL Node</title>
</head>
<body>
  <h1>Bem-vindo ao Dashboard JSCL (Node + Prisma)</h1>
  <p>Usuário: <%= user ? user.name : 'Visitante' %></p>
  <p>Unidade: <%= unidade ? unidade.nome : 'N/A' %></p>
  <p>Membros:</p>
  <ul>
    <% membros.forEach(function(membro) { %>
      <li><%= membro.nome %> (<%= membro.email %>)</li>
    <% }) %>
  </ul>
</body>
</html>
